// Placeholder game.js with respawn + collision system.
export function createGame(c){return{onResize(){},onPress(){},onRelease(){},update(){},render(ctx){ctx.fillStyle='#000';ctx.fillRect(0,0,c.width,c.height);}};}